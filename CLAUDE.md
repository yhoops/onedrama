# CLAUDE.md

## `/grill-with-docs`（及 `grilling`）问诊：一问一答，每题必须带选项

用这个技能问诊时，**不要**按技能默认的「一次把整轮 frontier 问完」（技能正文里那种一次抛
Q1–Q8 的写法）。

- **一次只问一个问题**，等用户答复后再问下一个。题目多了对用户是审阅负担。
- **每题必须给可选项**。用 `AskUserQuestion`：推荐项放第一个、label 标「推荐」；选项正文写清
  取舍与代价；能画示意图就用 `preview`（仅单选支持）。
- **事实自己查**。版本号、上游字段、真机行为、文件在哪这类要查证的，先自己查完再问——不要
  把能查到的东西拿去问用户。只把「决定权在用户」的取舍抛出去。
